// Pinging /healthcheck gives an idea of whether the application is working,
// without wasting time generating a page like the pages users are expected to see.
const HealthCheck = () => {
  return <p>This page is working correctly!</p>
}

export default HealthCheck
